/**
 * Placeholder for shared React UI primitives.
 *
 * For TabSwitch v0, app-specific components live in `apps/web/components/`.
 * Once a pattern is reused outside `apps/web`, promote it here.
 *
 * Example future moves: Button, Card, Toast — currently in apps/web/components/ui.
 */
export {};
